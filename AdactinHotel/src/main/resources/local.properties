# The set of local properties to execute the tests !

# The default browser to run - chrome / edge / firefox 
browser = chrome

# The mode at which the browser runs
headless = false

# The default timeout
timeout = 30000

# Max Retry on failure
max.retry = 2